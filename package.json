{
  "scripts": {
    "build": "node ./build.js",
    "fmt": "prettier --write .",
    "fmt:check": "prettier --check ."
  },
  "gitHooks": {
    "pre-commit": "prettier --write . && git add -A"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "prettier": "^2.6.0",
    "smartweave": "^0.4.46",
    "typescript": "^4.6.2",
    "yorkie": "^2.0.0"
  }
}
